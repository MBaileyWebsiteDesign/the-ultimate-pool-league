node_modules/
client/dist/
client/dist-demo/
server/src/data/db.json
# Generated by server/scripts/export-demo-data.js from a fresh seed - the
# GitHub Actions demo-deploy workflow regenerates this on every build, so it
# never needs to be (and shouldn't be) committed with real content.
client/src/demo/demoData.json
*.log
.DS_Store
*.timestamp-*.mjs
