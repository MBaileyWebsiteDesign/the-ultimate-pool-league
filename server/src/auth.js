// Removed: this used to hold the single hardcoded admin login (separate from
// player accounts). Login is unified now - see userAuth.js, which every
// account (including admins) authenticates through. This file is deleted
// from the repo on GitHub; it only lingers here if your local checkout is
// out of sync with a fresh `git pull`.
export {};
