// Removed: superseded by PlayerPortal.jsx (adds a "My Fixtures" panel and
// works off the single unified AuthContext instead of the old, separate
// PlayerAuthContext). This file is deleted from the repo on GitHub; it only
// lingers here if your local checkout is out of sync with a fresh `git pull`.
export {};
