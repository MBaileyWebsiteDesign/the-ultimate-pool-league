// Removed: this pub/sub kept two separate login sessions (admin, player)
// mutually exclusive. There's only one session now (see AuthContext.jsx),
// so there's nothing to coordinate. This file is deleted from the repo on
// GitHub; it only lingers here if your local checkout is out of sync with a
// fresh `git pull`.
export {};
